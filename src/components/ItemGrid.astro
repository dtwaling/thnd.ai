---
interface Props {
    id: string;
    icon?: string;
}
---

<!-- ToDo: Consider expanding on grid-list elements. -->
<ul class="grid-list not-content">
    <slot />
</ul>

<style>
    .grid-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, max-content));
        gap: 1rem;
        padding-left: 0;
    }
    .grid-list :global(> *) {
    }
    ul {
        list-style: none;
    }
</style>
